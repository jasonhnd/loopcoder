# v0.8.0 Go/No-Go Report

This is the binding release-evidence record for LoopCoder v0.8.0.

Current decision: **NO-GO**. The candidate code and release documentation are
being assembled, but signed staged artifacts, final self-bootstrap evidence,
protected-environment approval, publication, and post-publication verification
must be recorded here before the decision can become GO.

## Release Contract

- Supported tuple: native macOS Apple Silicon (`darwin/arm64`) only.
- Unsupported: Windows, Linux/Ubuntu, WSL, containers used as a LoopCoder
  runtime, Intel macOS, and Rosetta/amd64 macOS.
- Legacy fallback: v0.7.0, the final legacy multi-platform release.
- Binary asset: `loopcoder_0.8.0_darwin_arm64.tar.gz` only.
- Integrity material: `SHA256SUMS`, `SHA256SUMS.sigstore`, and the release
  workflow's verifiable signing/provenance evidence.
- Required PR checks: `verify`, `test`, `race`, and `security` on pinned
  `macos-15`, with an asserted Darwin arm64 Go host.
- Publication authority: protected `release-publication` environment with a
  required human reviewer.

## Candidate Identity

```text
tag: v0.8.0
candidate SHA: <final-main-sha>
pre-prod SHA: <matching-final-pre-prod-sha>
archive: loopcoder_0.8.0_darwin_arm64.tar.gz
archive SHA-256: <sha256>
checksum signature identity: https://github.com/jasonhnd/loopcoder/.github/workflows/release.yml@refs/tags/v0.8.0
checksum signature issuer: https://token.actions.githubusercontent.com
release workflow run: <url>
integrated main CI run: <url>
```

The tag, `main`, staged draft, archive, checksums, signature bundle, smoke, and
published release must all bind to the same candidate. Any mismatch is NO-GO.

## Required Gates

| Gate | Required evidence | Status |
| --- | --- | --- |
| Scope freeze | No post-freeze feature work; only release-blocking fixes and release evidence. | pending final audit |
| Orchestration safety | #959 and #960-#968 closed; integrated Darwin arm64 detached/progress/wait/cost acceptance recorded. | pass on pre-prod candidate; rebind final SHA |
| Storage migration | #867 closed; fresh, v0.7, repeated, interrupted, corrupt, disk-full, backup, and rollback fixtures pass. | pass on pre-prod candidate; staged artifact proof pending |
| Platform contract | Runtime, installer, upgrade, CI, and release accept only Darwin arm64 before side effects. | pass on pre-prod candidate |
| Living documentation | README, changelog, release note, usage, architecture, stability, releasing, self-bootstrap, troubleshooting, and this report agree. | pass on #953; final publication fields pending |
| Required PR checks | `verify`, `test`, `race`, and `security` green on final candidate. | pending final SHA |
| Full release race | `go test -race -count=1 -timeout=20m ./...` in release build. | pending tag workflow |
| Security | SAST/audit, govulncheck, policy tests, and threat-model regression gates green. | pending final SHA |
| Self-bootstrap | Exact staged binary, zero paid calls, human/JSON evidence, repo-external state. | pass on #868 candidate; tag-workflow artifact binding pending |
| v0.7 upgrade | Published v0.7 creates schema 9; candidate plans, backs up, migrates, replays no-op, and copied backup opens in v0.7. | pending staged smoke |
| Artifact identity | One Darwin arm64 archive; checksum/signature/provenance verify; no unsupported binary asset. | pending staged draft |
| Release blockers | All issues carrying `release-blocker` are closed as completed or explicitly deferred outside v0.8.0. | pending |
| Branch protection | `main` requires exactly the four emitted checks; no legacy or unsupported context remains. | pending readback |
| Publication environment | Required human reviewer configured and approval recorded. | pending |
| Failure behavior | Failed/cancelled run preserves a diagnosable draft and does not claim success. | pending workflow evidence |
| Post-publication verification | Public archive, checksum, signature, version stamp, install, upgrade, and latest-release state independently verified. | pending publication |

## Mandatory Commands

Run against the final candidate and staged artifact:

```text
go vet ./...
staticcheck ./...
go test ./...
go test -race -count=1 -timeout=20m ./...
go run ./cmd/loopcoder audit --repo . --layer sast
pwsh scripts/self-bootstrap-smoke.ps1 -Version 0.8.0 -Binary <staged-binary> -KeepArtifacts
pwsh scripts/release-smoke.ps1 -Version v0.8.0 -PreviousVersion 0.7.0
```

The default acceptance path must not call a paid provider or require private
provider credentials. Optional live-provider diagnostics are separate evidence
and cannot convert a failed deterministic gate into GO.

## Migration And Rollback Review

Before GO, verify that:

1. migration planning creates or modifies no file;
2. apply creates one owner-only, integrity-checked schema-9 backup;
3. schema versions 10 through 30 commit atomically;
4. repeated apply returns `no-op`;
5. all LoopCoder processes are stopped before apply or rollback;
6. rollback copies rather than mutates the backup;
7. the published v0.7.0 binary opens the copied backup; and
8. the operator acknowledges that rollback discards v0.8-only state.

Never point v0.7.0 at the schema-30 database.

## Final Evidence Block

Replace every placeholder and change the decision to GO only after publication
and independent public verification succeed.

```text
decision: NO-GO
final tag/SHA: <v0.8.0 -> sha>
main/pre-prod equality: <evidence>
required CI: <run URL; verify/test/race/security all success>
full release race: <run URL/job>
security: <run URL/job and audit summary>
tracked local-only files: <result>
docs policy and links: <commands/results>
self-bootstrap: <artifact hash, host tuple, result, evidence directory summary>
v0.7 upgrade/migration/rollback: <result>
draft release: <URL and immutable asset inventory>
archive SHA-256: <sha256>
signature verification: <identity, issuer, result>
branch protection readback: <four contexts, strict, PR required, approval count 0, admins enforced>
publication environment: <required reviewer and approval>
publish result: <release URL, published_at, draft=false, prerelease=false>
post-publication smoke: <command/result>
open release blockers: 0
human reviewer: <GitHub login>
notes: <remaining non-blocking limitations only>
```
