# v0.7.0 Go/No-Go Report

This is the release-readiness report template for issue #655. Attach the
completed report to the release readiness PR or issue before tagging v0.7.0.

Current decision: **NO-GO** until the human release gate confirms every required
item below. This document is intentionally truthful before publication: it does
not claim v0.7.0 is installable until the tag, signed checksums, and assets
exist.

## Release Truth

- Candidate version: `v0.7.0`
- Current customer install target before v0.7.0 assets exist: `v0.6.1`
- Tag created: no
- GitHub Release published: no
- Signed checksums verified: no
- Release assets verified: no

## Required Gates

Record evidence next to each item before changing the decision to GO:

| Gate | Evidence | Status |
| --- | --- | --- |
| v0.7.0 milestone issues closed or explicitly deferred | GitHub milestone review | pending |
| CI green | release-readiness PR checks | pending |
| No local-only files tracked | `git ls-files .loopcoder` returns empty | pending |
| Command docs inventory | `go test ./internal/cli -run TestCommandDocs` | pending |
| Doctor/report JSON tests | targeted Go tests for `internal/doctor`, `internal/cli`, `internal/runstatus`, `internal/reportquery` | pending |
| Storage permission hardening | Unix permission tests plus Windows ACL warning tests for `$LOOPCODER_HOME/data/loopcoder.db` | pending |
| Upgrade/migration tests | targeted Go tests for `internal/upgrade` and `internal/migrate` | pending |
| Source validation | `go vet ./...` plus targeted tests; do not use the known-hanging full `go test ./...` worker path | pending |
| Self-bootstrap acceptance | `pwsh scripts/self-bootstrap-smoke.ps1` | pending |
| Release artifact smoke | `pwsh scripts/release-smoke.ps1 -Version 0.7.0` after assets exist | pending |
| v0.6.1 upgrade path | release smoke verifies `-PreviousVersion 0.6.1` | pending |

## Manual Checklist

Before GO:

1. Confirm all v0.7.0 milestone issues are closed or explicitly deferred in the
   release PR or issue.
2. Confirm no tag or GitHub Release was created by automation before human
   approval.
3. Confirm `SHA256SUMS` and `SHA256SUMS.sigstore` exist on the v0.7.0 release
   and verify with the expected GitHub Actions identity.
4. Confirm every platform archive listed in `SHA256SUMS` exists and its hash
   matches.
5. Confirm install/archive extraction, `doctor --format json`, `report`,
   projects registry, database path outside the repo, dry-run migration,
   provider compatibility, nested run-tree observability, already-latest
   upgrade, and v0.6.1-to-v0.7.0 upgrade smoke all passed.
6. Confirm rollback instructions are explicit: select a prior released binary,
   keep or back up `$LOOPCODER_HOME`, and do not delete repo-local `.loopcoder/`
   history unless the operator explicitly chooses to.

## Evidence Block

Fill this in for the final readiness PR or issue:

```text
decision: GO | NO-GO
milestone review:
ci:
tracked local-only files:
command docs inventory:
doctor/report JSON tests:
storage permission hardening:
upgrade/migration tests:
self-bootstrap smoke:
release smoke:
signed checksum verification:
asset verification:
v0.6.1 upgrade verification:
rollback/cleanup review:
human reviewer:
notes:
```
